import tensorflow as tf
import numpy as np

# def csds(tensor):
#     static_tensor_shape = tensor.shape.as_list()
#     dynamic_tensor_shape = tf.shape(tensor)
#     combined_shape = []
#     for index, dim in enumerate(static_tensor_shappe):